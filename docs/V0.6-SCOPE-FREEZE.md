# Link2Context V0.6 Scope Freeze / 范围冻结

## 1. 最朴素、最高优先级的产品目标

> **只要用户给出一个链接，当前 AI 就应该能够读取这个链接里的信息；不论信息是什么类型，都尽可能完整地交给 AI。任何无法取得或无法交付的信息都必须明确暴露，禁止静默丢失后假装“完整”。**

这是 V0.6 的唯一产品 North Star（北极星目标）。实现细节都服务于它，不反过来绑架目标。

工程化表达：

```text
URL/resource
  → safe acquisition
  → type-aware decoding
  → structured context + original assets
  → target-aware delivery
  → explicit completeness/evidence state
```

## 2. “完整”的 V0.6 定义

V0.6 不把“完整”定义成无限抓取整个互联网，而定义成以下可验证原则：

1. **能识别就不压扁**：正文、标题、层级、列表、代码、表格、链接、图注、关键图片、原始文件分别保留语义。
2. **能保留原件就不伪造转换**：PDF、图片、Office、音视频、压缩包等二进制继续保留原文件交付，不把乱码冒充文本。
3. **动态内容有限但真实地渲染**：在用户明确授权浏览器上下文后，允许受限 DOM settle / scroll / load-more；不无界滚动、不盲点任意控件。
4. **分页宁缺毋错**：下一页除 URL/控件信号外，还要通过 Article Identity（文章身份）检查，防止静默串入下一篇文章。
5. **图文不能静默缺图**：正文关键图片尝试实际下载并附件交付；失败会标记 partial（部分完成）并禁止自动发送。
6. **编码不能默默乱码**：保存 charset 决策来源与置信度；HTTP 未声明时继续检查 BOM / HTML/XML 声明 / UTF-8 合法性。
7. **目标 AI 能力不假装永久真值**：每项能力区分 planned / Link2Context-safe / live-verified，并带验证日期。
8. **外部内容永远是不可信数据**：网页里的“忽略之前指令”等文字属于 source data，不升级成系统/用户指令。
9. **Auto-send 是可选便利功能，不是内容完整性的前提**：默认手动；自动发送必须取得独立成功证据，否则明确 `SEND_UNCONFIRMED`。
10. **任何 partial / unsupported / unverified 都必须可见**：不能用“任务完成”掩盖缺失。

## 3. V0.6 已冻结的实现边界

V0.6 做：

- Mozilla Readability + DOM structured walker；
- canonical Context Model + provenance/trust；
- heading / paragraph / list / blockquote / code / table / link / image / attachment；
- `src` / `srcset` / 常见 lazy image 属性；
- 关键图片筛选、下载、大小/数量预算、实际 MIME 验证；
- 传统分页 + Article Identity + 去重；
- 有授权前提的 bounded rendered acquisition（有限渲染采集）；
- BOM / HTTP / document-declaration / UTF-8 validity 编码链；
- ChatGPT / DeepSeek / 豆包 / 千问 Target Profile；
- V0.6 通用 target handoff；
- 千问保留 V0.5.3 已实测 CDP 真输入路径；
- ChatGPT / DeepSeek / 豆包自动发送采用 button → form → **仅显式 Auto-send 时** bounded CDP Enter fallback；
- 原 V0.5.3 special-source / original-binary 稳定路径作为 fallback，不为架构漂亮而重写已验证能力；
- 固定 live evidence（真实浏览器证据）账本。

V0.6 明确不做：

- 绕过登录/验证码/付费墙/DRM；
- 无限滚动全网通吃；
- 自动理解音视频内容本身（保留/交付原文件是本版边界）；
- 给所有 AI 绑定脆弱私有 API；
- 把 `debugger` 暴露成通用浏览器自动化接口；
- 因为某家 AI 当前支持某格式，就声称永久支持；
- 为了 100% 网页覆盖继续无限增加站点补丁。

## 4. V0.6 Release Candidate / 发布候选停止开发条件

当以下代码条件全部成立，**V0.6 功能开发停止**，进入 live acceptance（真实浏览器验收）而不是继续加新功能：

- [x] Canonical Context Model；
- [x] provenance / trust + indirect prompt injection 边界；
- [x] Mozilla Readability pinned reuse；
- [x] structured DOM extraction；
- [x] image inventory + bounded image asset acquisition；
- [x] pagination identity/dedupe；
- [x] bounded rendered acquisition state machine；
- [x] charset decision chain；
- [x] Target Profiles + capability evidence states；
- [x] V0.6 generic handoff runtime；
- [x] V0.6 Qianwen structured/mixed-media runtime；
- [x] optional auto-send fail-closed fallback ladder；
- [x] V0.5.3 proven fallback paths retained；
- [x] fixed live evidence format；
- [ ] full CI green on final candidate commit；
- [ ] final security/attack self-review has no merge-blocking P0/P1；
- [ ] branch docs / changelog / RC notes aligned to actual code.

完成最后三项后：

> **STOP DEVELOPMENT.**
>
> 不再继续加“顺手功能”，只允许针对真实验收暴露的 regression / blocker（回归/阻断问题）做修复。

## 5. 合并仍需要真实验收

“停止开发”不等于“立刻合并”。

PR #11 在以下 live gate 完成前保持 Draft：

- ChatGPT manual text handoff PASS；
- DeepSeek manual text handoff PASS；
- 豆包 manual text handoff PASS；
- 千问 V0.6 manual text handoff PASS，并保持 V0.5.3 可编辑/可删除回归不复发；
- 至少一个图文页面证明正文 + 关键图片实际同时交付；
- PDF/图片原文件路径不回归；
- Auto-send 按 target 单独记录，未验证/失败不得假装成功；
- STOP 关键阶段至少做一轮真实 smoke。

真实验收只修 blocker，不重新打开 V0.6 scope。
